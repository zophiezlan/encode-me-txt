/**
 * Thin shim — encoderConfig has been split into per-category modules.
 * All exports are re-exported from ./encoderConfig/index.js so existing
 * imports continue to work unchanged.
 */
export * from "./encoderConfig/index.js";
